filebeat.config:
  modules:
    path: ${path.config}/modules.d/*.yml
    reload.enabled: false

filebeat.autodiscover:
  providers:
    - type: docker
      hints.enabled: true
      templates:
        #- condition:
                #contains:
                        #container.image.name: docker/compose
          #config:




processors:
- add_cloud_metadata: ~

output.elasticsearch:
  hosts: '${ELASTICSEARCH_HOSTS:http://es-1:9200}'

#setup.dashboards.enabled: true
#setup.kibana:
#   host: "http://es-1:5601"

